---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: clusterserviceaccountcredentials.stackit-operator.webhippie.de
spec:
  group: stackit-operator.webhippie.de
  names:
    kind: ClusterServiceAccountCredentials
    listKind: ClusterServiceAccountCredentialsList
    plural: clusterserviceaccountcredentials
    singular: clusterserviceaccountcredentials
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.projectId
      name: ProjectID
      type: string
    - jsonPath: .status.connected
      name: Connected
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterServiceAccountCredentials is the Schema for the clusterserviceaccountcredentials
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterServiceAccountCredentials
            properties:
              privateKey:
                description: |-
                  privateKey references a secret or direct value containing a custom RSA
                  private key matching the public key uploaded for the service account
                  key. Only relevant when serviceAccountKey uses a custom key pair.
                  Since this resource is cluster-scoped, secretKeyRef.namespace must be
                  set explicitly.
                properties:
                  secretKeyRef:
                    description: secretKeyRef selects a key of a Kubernetes Secret.
                    properties:
                      key:
                        description: key is the key of the Secret to select from.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                      namespace:
                        description: namespace of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  value:
                    description: value directly specifies the value as a plain string.
                    type: string
                type: object
              projectId:
                description: |-
                  projectId is the STACKIT project ID that resources referencing these
                  credentials are associated with.
                type: string
              region:
                description: |-
                  region is the default STACKIT region used for regional services
                  (e.g. Postgres Flex) when a resource does not override it.
                type: string
              serviceAccountKey:
                description: |-
                  serviceAccountKey references a secret or direct value containing the
                  full JSON service account key (Key Flow), as generated via the STACKIT
                  Portal or the stackit_service_account_key resource. Since this
                  resource is cluster-scoped, secretKeyRef.namespace must be set
                  explicitly. Either serviceAccountKey or token must be set.
                properties:
                  secretKeyRef:
                    description: secretKeyRef selects a key of a Kubernetes Secret.
                    properties:
                      key:
                        description: key is the key of the Secret to select from.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                      namespace:
                        description: namespace of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  value:
                    description: value directly specifies the value as a plain string.
                    type: string
                type: object
              token:
                description: |-
                  token references a secret or direct value containing a long-lived
                  STACKIT service account access token (Token Flow). This flow is
                  deprecated upstream but supported for simple setups. Since this
                  resource is cluster-scoped, secretKeyRef.namespace must be set
                  explicitly. Either serviceAccountKey or token must be set.
                properties:
                  secretKeyRef:
                    description: secretKeyRef selects a key of a Kubernetes Secret.
                    properties:
                      key:
                        description: key is the key of the Secret to select from.
                        type: string
                      name:
                        description: name is the name of the Secret.
                        type: string
                      namespace:
                        description: namespace of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  value:
                    description: value directly specifies the value as a plain string.
                    type: string
                type: object
            required:
            - projectId
            type: object
          status:
            description: status defines the observed state of ClusterServiceAccountCredentials
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ClusterServiceAccountCredentials resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connected:
                description: |-
                  connected shows if the STACKIT API was reached successfully using
                  these credentials.
                type: boolean
            required:
            - connected
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
